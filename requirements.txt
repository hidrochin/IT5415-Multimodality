# ── Core MVP (Phase 1: text RAG, portable Windows / Colab) ──────────────
pymupdf>=1.24.0                # PDF text + image extraction (import fitz)
sentence-transformers>=3.0.0  # text embeddings (pulls torch)
faiss-cpu>=1.8.0              # vector index (swap for faiss-gpu on Colab if wanted)
google-genai>=1.0.0          # Gemini API (figure captions + grounded QA)
python-dotenv>=1.0.0         # load GEMINI_API_KEY from .env
pyyaml>=6.0                  # config.yaml
numpy>=1.26.0
Pillow>=10.0.0               # image handling
tqdm>=4.66.0                 # progress bars

# ── Phase 2 (multimodal: OCR + figure captions) ─────────────────────────
paddlepaddle==3.0.0           # PaddleOCR backend (CPU). Pin: 3.3.x has a Windows
                              #   PIR/oneDNN executor regression that breaks OCR.
paddleocr>=3.0.0              # OCR over extracted figures/screenshots (module 6.2)
# Figure captioning (module 6.3) reuses google-genai, already listed above.

# ── Later phases (image embeddings, alt rerank, UI) ──────────────────────
# Uncomment as each module is added. Some are heavy / install more easily on Colab.
# open_clip_torch>=2.24.0    # CLIP / SigLIP image embeddings
# FlagEmbedding>=1.2.0       # BGE-M3 + bge-reranker-base
# pdfplumber>=0.11.0         # table-aware parsing
# gradio>=4.0.0              # UI
